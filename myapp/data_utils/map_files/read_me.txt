# map_files/

Contains static lookup tables used throughout BrainPowerX to convert between
internal pipeline identifiers (file names, method codes) and UI-facing strings.

Each map lives in its own file. To add a new method or outcome, edit the
relevant file here — no function logic elsewhere needs to change.